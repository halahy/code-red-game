# GitHub Pages 部署指南（免费永久）

## 📋 准备工作

1. 注册GitHub账号：https://github.com（如果还没有）

## 🚀 部署步骤

### 方法A：网页上传（最简单）

1. **创建新仓库**
   - 登录GitHub
   - 点击右上角 "+" → "New repository"
   - 仓库名：`code-red-game`（可以改成任何名字）
   - 选择 **Public**（公开）
   - ✅ 勾选 "Add a README file"（可选）
   - 点击 "Create repository"

2. **上传文件**
   - 在仓库页面，点击 "uploading an existing file"
   - 将 `web` 文件夹里的**所有文件**拖进去
     - 包括：index.html, css文件夹, js文件夹等
   - 在底部输入提交信息："Initial commit"
   - 点击 "Commit changes"

3. **启用GitHub Pages**
   - 点击仓库顶部的 "Settings"
   - 左侧菜单找到 "Pages"
   - 在 "Source" 下：
     - Branch 选择：`main`
     - Folder 选择：`/ (root)`
   - 点击 "Save"
   - 等待1-2分钟

4. **获得网址**
   - 回到 "Settings" → "Pages"
   - 你会看到你的网址：
     ```
     https://你的用户名.github.io/code-red-game/
     ```
   - 例如：`https://zhangsan.github.io/code-red-game/`

5. **分享链接**
   - 把这个网址发给朋友，他们就可以在手机上直接玩了！

---

### 方法B：使用Git命令（适合有Git的用户）

```bash
# 1. 进入web文件夹
cd web

# 2. 初始化Git
git init

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "Initial commit"

# 5. 创建main分支
git branch -M main

# 6. 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/code-red-game.git

# 7. 推送
git push -u origin main
```

然后按照方法A的第3-5步启用GitHub Pages。

---

## ✨ 更新游戏

如果以后修改了游戏代码：

1. 在GitHub网页上直接编辑文件，或
2. 使用Git命令：
   ```bash
   cd web
   git add .
   git commit -m "Update game"
   git push
   ```
   
更新后，网站会自动更新（可能需要等1-2分钟）

---

## 💡 优势

- ✅ **完全免费**
- ✅ **永久有效**
- ✅ **全球CDN加速**
- ✅ **自动HTTPS**
- ✅ **可以自定义域名**（高级功能）

---

## 🆘 常见问题

**Q: 网址显示404？**
A: 等待2-3分钟，GitHub Pages需要时间部署

**Q: 如何修改网址？**
A: 修改仓库名称即可（Settings → 最下方 → Change repository name）

**Q: 可以删除吗？**
A: 可以，在Settings最下方删除仓库即可

---

**就这么简单！** 🎮

